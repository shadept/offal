/** Data for the shared tooltip component. */
export interface TooltipData {
  name: string;
  iconUrl?: string;
  conditions: string[];
  stats: string;
  coverage?: string;
  capacities?: string;
  description?: string;
}
